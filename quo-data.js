function renderSettings(){const x=S.settings;return pageHead('Settings','Only business and document defaults. Bank, account, Viber and hotline are locked to White Saffron values.','<button class="btn primary" data-save-settings>Save Settings</button>')+`<div class="settings-grid"><section class="editor-card"><header><span class="section-no">01</span><h3>Business Details</h3></header><div class="card-body"><div class="form-grid"><div class="field full"><label>Business Name</label><input data-setting="company_name" value="${esc(x.company_name)}"></div><div class="field full"><label>Address</label><input data-setting="address" value="${esc(x.address)}"></div><div class="field"><label>Hotline</label><input value="${FIXED.hotline}" readonly></div><div class="field"><label>Email</label><input data-setting="email" value="${esc(x.email)}"></div></div></div></section><section class="editor-card"><header><span class="section-no">02</span><h3>Bank Details</h3></header><div class="card-body"><div class="form-grid"><div class="field full"><label>Bank</label><input value="${esc(FIXED.bank)}" readonly></div><div class="field"><label>Account Name</label><input data-setting="account_name" value="${esc(x.account_name||x.company_name)}"></div><div class="field"><label>Account Number</label><input value="${FIXED.account}" readonly></div><div class="field"><label>Payment Slip Viber</label><input value="${FIXED.viber}" readonly></div></div></div></section><section class="editor-card"><header><span class="section-no">03</span><h3>Document Settings</h3></header><div class="card-body"><div class="form-grid"><div class="field"><label>Currency</label><input data-setting="currency" value="${esc(x.currency)}"></div><div class="field"><label>Default Validity Days</label><input data-setting="default_validity_days" type="number" min="0" value="${num(x.default_validity_days)}"></div><div class="field full"><label>Number Format</label><input value="QT-2026-0001 / PI-2026-0001 / INV-2026-0001 / RC-2026-0001" readonly></div></div></div></section><section class="editor-card"><header><span class="section-no">04</span><h3>Default Terms & PDF</h3></header><div class="card-body"><div class="form-grid"><div class="field full"><label>Quotation Terms</label><textarea data-setting="quotation_terms">${esc(x.quotation_terms||'')}</textarea></div><div class="field full"><label>Invoice Terms</label><textarea data-setting="invoice_terms">${esc(x.invoice_terms||'')}</textarea></div><div class="field full"><label>PDF Footer</label><input data-setting="footer" value="${esc(x.footer||defaultSettings.footer)}"></div></div><div class="settings-note" style="margin-top:10px">Paper size is fixed to A4 for clean browser PDF export. Catering menus are generated as the second PDF page only when Include Menu Page is enabled.</div></div></section></div>`}
function blankDoc(type='quotation',copy=null){const base={id:null,document_number:'NEW',document_type:type,status:type==='receipt'?'Issued':'Draft',currency:S.settings.currency||'MVR',creation_date:'',expires_on:'',customer_name:'',customer_phone:'',customer_address:'',event_name:'',service_enabled:false,service_type:'Catering - Buffet',service_from:'',service_to:'',service_pax:0,service_label:'Service Period',venue:'',items:[{description:type==='receipt'?'Payment received':'',qty:1,unit:type==='receipt'?'Payment':'Pax',price:0}],gst_mode:'none',gst_rate:8,discount:0,show_gst:false,include_menu:false,menu_title:'CATERING MENU',menu_text:'',use_advance:false,advance_percent:50,advance_due:'',bank:FIXED.bank,account_no:FIXED.account,slip_via:'Viber',slip_contact:FIXED.viber,paid_amount:0,payment_reference:'',extra_terms:type==='invoice'?(S.settings.invoice_terms||''):(S.settings.quotation_terms||''),source_document_id:null,created_by_name:'',created_at:null,updated_by_name:'',updated_at:null,version:1};if(copy){Object.assign(base,JSON.parse(JSON.stringify(copy)));base.id=null;base.document_number='NEW';base.document_type=type;base.status=type==='receipt'?'Issued':'Draft';base.created_by_name='';base.created_at=null;base.updated_by_name='';base.updated_at=null;base.version=1;base.source_document_id=copy.id||null;if(type!=='receipt'){base.paid_amount=0;base.payment_reference=''}}return base}
function openEditor(d){S.current=JSON.parse(JSON.stringify(d));S.view='editor';S.convertOpen=false;render();scrollTo(0,0)}
function newDocument(type){S.current=blankDoc(type);S.view='editor';S.filter=type;closeModal();render();scrollTo(0,0)}
function editorStatusOptions(d){const values=[...new Set([...(CFG[d.document_type]?.statuses||[]),d.status].filter(Boolean))];return values.map(v=>`<option ${v===d.status?'selected':''}>${esc(v)}</option>`).join('')}
function eventFields(d){return `<div class="switch-row"><div class="switch-copy"><b>Include Event / Service</b><span>Use for catering, buffet, events or dated services.</span></div><label class="toggle"><input data-field="service_enabled" type="checkbox" ${d.service_enabled?'checked':''}><i></i></label></div><div class="form-grid three ${d.service_enabled?'':'hidden'}" id="eventFields"><div class="field"><label>Event</label><input data-field="event_name" value="${esc(d.event_name||'')}" placeholder="Meal Package"></div><div class="field"><label>Service Type</label><input data-field="service_type" value="${esc(d.service_type||'')}"></div><div class="field"><label>Guests</label><input data-field="service_pax" type="number" min="0" value="${num(d.service_pax)}"></div><div class="field"><label>From</label><input data-field="service_from" type="date" value="${esc(d.service_from||'')}"></div><div class="field"><label>To</label><input data-field="service_to" type="date" value="${esc(d.service_to||'')}"></div><div class="field"><label>Venue</label><input data-field="venue" value="${esc(d.venue||'')}"></div></div>`}
function itemEditor(d){return `<div class="item-lines"><div class="item-head"><span>Description</span><span>Qty</span><span>Unit</span><span>Rate</span><span style="text-align:right">Amount</span><span></span></div>${(d.items||[]).map((i,n)=>`<div class="item-row" data-item="${n}"><input data-item-field="description" value="${esc(i.description||'')}" placeholder="Description"><input data-item-field="qty" type="number" step="0.01" value="${num(i.qty)}"><input data-item-field="unit" value="${esc(i.unit||'')}" placeholder="Pax"><input data-item-field="price" type="number" step="0.01" value="${num(i.price)}"><span class="amount">${moneyOnly(num(i.qty)*num(i.price))}</span><button class="remove-line" data-remove-item="${n}" type="button">×</button></div>`).join('')}</div><div class="items-foot"><button class="btn" data-add-item type="button">+ Add Line</button><div class="totals-mini">Document Total <b>${money(calc(d).total,d.currency)}</b></div></div><div class="form-grid three" style="margin-top:10px"><div class="field"><label>GST Treatment</label><select data-field="gst_mode"><option value="none" ${d.gst_mode==='none'?'selected':''}>No GST</option><option value="exclusive" ${d.gst_mode==='exclusive'?'selected':''}>Add GST</option><option value="inclusive" ${d.gst_mode==='inclusive'?'selected':''}>Price Includes GST</option></select></div><div class="field"><label>GST %</label><input data-field="gst_rate" type="number" step="0.01" value="${num(d.gst_rate)}"></div><div class="field"><label>Discount</label><input data-field="discount" type="number" step="0.01" value="${num(d.discount)}"></div></div>`}
function paymentSection(d){const c=calc(d);if(d.document_type==='receipt')return `<div class="form-grid"><div class="field"><label>Amount Received</label><input value="${money(c.total,d.currency)}" readonly></div><div class="field"><label>Payment Reference</label><input data-field="payment_reference" value="${esc(d.payment_reference||'')}" placeholder="Transfer / cash reference"></div><div class="field full"><label>Related Document</label><input value="${esc(sourceNo(d.source_document_id)||'Standalone receipt')}" readonly></div></div>`;if(d.document_type==='invoice')return `<div class="form-grid three"><div class="field"><label>Total</label><input value="${money(c.total,d.currency)}" readonly></div><div class="field"><label>Paid</label><input data-field="paid_amount" type="number" step="0.01" min="0" value="${num(d.paid_amount)}"></div><div class="field"><label>Balance Due</label><input value="${money(c.balance,d.currency)}" readonly></div><div class="field full"><label>Payment Reference</label><input data-field="payment_reference" value="${esc(d.payment_reference||'')}" placeholder="Optional reference"></div></div><div class="settings-note" style="margin-top:10px">Bank Transfer: ${esc(FIXED.bank)} - Account No.: ${FIXED.account}. Send payment slip via Viber to ${FIXED.viber}.</div>`;return `<div class="switch-row"><div class="switch-copy"><b>Advance Required</b><span>Enable only when this document requires an advance payment.</span></div><label class="toggle"><input data-field="use_advance" type="checkbox" ${d.use_advance?'checked':''}><i></i></label></div><div class="form-grid ${d.use_advance?'':'hidden'}" id="advanceFields"><div class="field"><label>Advance %</label><input data-field="advance_percent" type="number" min="0" max="100" value="${num(d.advance_percent)}"></div><div class="field"><label>Payment Due</label><input data-field="advance_due" type="date" value="${esc(d.advance_due||'')}"></div></div><div class="settings-note" style="margin-top:10px">Bank Transfer: ${esc(FIXED.bank)} - Account No.: ${FIXED.account}. After payment, send the payment slip via Viber to ${FIXED.viber}.</div>`}
function sourceNo(id){return S.docs.find(x=>x.id===id)?.document_number||''}
